# package-categorization

Utilities and services for categorizing transactions/statements.

## Usage

```python
from package_categorization import CategorizationService

svc = CategorizationService()
assert svc.health() == {"status": "ok"}
```
